// ここに即時実行関数を実装してください。

// ========================================
// 1. アプリの初期化（即時実行関数）
// ========================================
// 即時実行関数を使って以下を実装：
// - appName = 'マイアプリ', version = '1.0.0', author = '開発者' を定義
// - app-info 要素に上記の情報を表示
// - console.log で「初期化が完了しました」と出力

// ========================================
// 2. 計算結果（即時実行関数 + 引数 + 戻り値）
// ========================================
// 即時実行関数を使って以下を実装：
// - 引数 a と b を受け取る
// - sum (a + b) と product (a * b) を計算
// - { sum: sum, product: product } を返す
// - 戻り値を calcResult 変数に代入
// - calc-result 要素に結果を表示
// - 引数として 15 と 4 を渡す

// ========================================
// 3. プライベート変数を持つカウンター（即時実行関数）
// ========================================
// 即時実行関数を使って counter オブジェクトを作成：
// - プライベート変数 count を 0 で初期化
// - 以下のメソッドを返す：
//   - increment(): count を 1 増やして return
//   - decrement(): count を 1 減らして return
//   - getValue(): count を return
//   - reset(): count を 0 にして return

// ========================================
// UI関数（グローバルに公開）
// ========================================
// incrementCounter() - counter.increment() を呼んで counter-display を更新
// decrementCounter() - counter.decrement() を呼んで counter-display を更新
// showCount() - counter.getValue() を呼んで alert で表示
