// ここに関数式を使った関数を実装してください。
// すべて「const 変数名 = function() { };」の形式で書いてください。

// ========================================
// 関数式の実装
// ========================================

// greet(name)
// - 関数式で定義
// - 'こんにちは、' + name + 'さん！' を返す

// add(a, b)
// - 関数式で定義
// - a + b を返す

// subtract(a, b)
// - 関数式で定義
// - a - b を返す

// multiply(a, b)
// - 関数式で定義
// - a * b を返す

// divide(a, b)
// - 関数式で定義
// - b が 0 なら 0 を返す、そうでなければ a / b を返す

// ========================================
// UI関数（これらも関数式で実装）
// ========================================

// testGreet()
// - 関数式で定義
// - nameInput から名前を取得
// - greet() を呼び出して結果を取得
// - greetResult に結果を表示

// testCalculator()
// - 関数式で定義
// - num1Input と num2Input から数値を取得
// - add, subtract, multiply, divide をすべて実行
// - calcResult に結果をまとめて表示

// testFunctionAsValue()
// - 関数式で定義
// - func1 を関数式で定義（'Hello from func1' を返す）
// - func2 に func1 をコピー
// - 両方実行して結果を valueResult に表示
