// ここに以下の関数を実装してください：

// getInputName() - 入力された名前を取得して返す

// getInputMessage() - 入力されたメッセージを取得して返す

// createHeader(name) - 名前からヘッダーを作成して返す

// createFooter() - フッターを作成して返す

// createCard(name, message) - 上記の関数を呼び出してカード全体を作成して返す

// displayCard() - 上記の全関数を呼び出してカードを画面に表示する
